# Local Laptop Profile

`profile.template.json` is a source-controlled safe template for isolated local loopback work.

Actual local settings, if ever needed, belong under the external `LOXFS_CMD_SIG_LOCAL_ROOT\config\` tree and remain outside Git.
